version: '3.9'
services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: dayplanner-postgres
    environment:
      POSTGRES_DB: dayplanner
      POSTGRES_USER: dayplanner
      POSTGRES_PASSWORD: dayplanner
    ports:
      - '5432:5432'
    volumes:
      - dayplanner_pgdata:/var/lib/postgresql/data

volumes:
  dayplanner_pgdata:
